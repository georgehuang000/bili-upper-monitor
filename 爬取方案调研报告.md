# B站爬取方案调研报告：纯 API（"CLI"）方案 vs 复用已登录浏览器（CDP）方案

> 调研时间：2026-07-28。针对两个问题：
> 1. 不是有 B站 CLI / API 库吗，为什么不用？
> 2. 为什么每次要新开一个"空浏览器"，不能连我自己已经登录好的浏览器吗？

---

## 0. 先说结论（TL;DR）

| | 方案A：纯 API（无浏览器） | 方案B：CDP 连接你已登录的浏览器 | 现状：MediaCrawler 独立无头浏览器 |
|---|---|---|---|
| 需要开浏览器 | ❌ 完全不需要 | 需要你的 Chrome 一直开着 | 每次子进程启动一个无头 Chromium |
| 登录方式 | 从你的浏览器复制一次 Cookie | 直接复用，零配置 | 首次扫码一次，之后自动复用 |
| 无人值守定时任务（8:00/18:00） | ✅ 完美支持 | ⚠️ 基本不可行（见 3.3） | ✅ 支持 |
| 风控风险 | 中（无浏览器指纹，易 -352/412） | 低（指纹最真实），但风险绑定你的主账号 | 中低 |
| 速度/资源 | 最快最省 | 中 | 最重（每 UID 起一次浏览器） |
| 改造成本 | 低（`backend/` 里已有 80% 的代码） | 低（改 2 个配置项），但有副作用风险 | 0 |

**推荐**：主力切到**方案A（纯 API + 从你浏览器导出的 Cookie）**，MediaCrawler 保留作回退。方案B 不适合本项目的定时无人值守场景，且有"可能把你正在用的浏览器标签页关掉"的实际风险（见 3.4）。

另外澄清一点：**现状其实没有你想的那么糟**——扫码只需要一次（登录态存在 `external/MediaCrawler/browser_data/bili_user_data_dir`），且 `HEADLESS = True`，日常运行时浏览器是**无头**的，并不会真的弹窗口。你看到"空浏览器"应该是首次登录或调试时的现象。

---

## 1. 现状：为什么会"新开一个空浏览器"

当前链路（`server/crawler_runner.py`）：

```
scheduler(8:00/18:00) → 每个UP主 × {视频,动态} 各起一个子进程
  → MediaCrawler main.py --platform bili --type creator --lt qrcode
    → Playwright 启动一个 独立 profile 的 Chromium（HEADLESS=True）
    → 打开 bilibili.com 拿 Cookie + 从页面 localStorage 拿 WBI 签名密钥
    → 之后实际请求走 httpx 直接调 B站 Web API
```

关键点：

- 它是"空浏览器"是因为用的是**独立的用户数据目录**（`browser_data/bili_user_data_dir`），和你日常的 Chrome 完全隔离，所以第一次没有任何登录态，需要扫一次码；扫完后 `SAVE_LOGIN_STATE=True` 会把登录态存下来，后续复用。
- **浏览器在这套方案里的真实作用只有两个**：① 提供登录 Cookie；② 从页面 `localStorage` 里读 `wbi_img_urls` 用来做 WBI 签名（见 `media_platform/bilibili/client.py` 的 `get_wbi_keys()`）。真正的数据抓取全部是 httpx 直接调 API，**并不靠浏览器渲染页面**。
- 也就是说：浏览器是"拿钥匙的工具"，不是"干活的工具"。这正是两种替代方案的切入点——钥匙（Cookie + WBI key）完全可以用别的方式拿到。

一轮全量 = 7 个 UP主 × 2 种模式 = **14 次子进程 + 14 次浏览器启动**，这是当前方案最大的开销。

---

## 2. 方案A：纯 API 方案（你说的 "B站 CLI"）

### 2.1 "B站 CLI" 到底指什么

市面上叫 "bilibili cli" 的现成工具（BBDown、bilibili-cli 等）主要面向**视频下载/弹幕/终端看番**，不覆盖"监控 UP主动态 + 视频列表"这个场景，不能直接拿来用。真正对应的是：

1. **`bilibili-api` Python 库**（Nemo2011/bilibili-api，400+ 接口）：提供 `user.get_videos()`、`user.get_dynamics()`，内置 WBI 签名处理，用 `Credential(sessdata=…, bili_jct=…, buvid3=…)` 传登录态。
2. **直接调 B站 Web API**——本项目 `backend/crawler/bilibili.py` 里**已经实现过一版**：
   - 视频列表：`/x/space/wbi/arc/search`（WBI 签名已在 `backend/crawler/wbi.py` 实现）
   - 动态列表：`/x/polymer/web-dynamic/v1/feed/space`

### 2.2 为什么 `backend/` 那版当时没用起来

检查了 `backend/config.py` 的 `REQUEST_HEADERS`：**只有 UA，完全没带 Cookie**。而 B站这两个接口现状是：

- 动态接口 `feed/space`：**必须登录 Cookie**，匿名直接返回 `-352`（风控校验失败）。
- 视频列表 `arc/search`：即使 WBI 签名正确，无 Cookie（缺 `buvid3`/`bili_ticket` 等）也高概率 `-352` / `412`。

所以旧版失败的根因不是"纯 API 走不通"，而是**没带登录态**。这也是后来切到 MediaCrawler（用浏览器拿 Cookie）的原因。

### 2.3 纯 API 方案怎么补齐

只需要把"钥匙"从你已登录的浏览器里拿出来，一次性配置：

1. 在你日常浏览器 F12 → Application → Cookies → bilibili.com，复制 `SESSDATA`、`bili_jct`、`buvid3`（可选 `buvid4`、`DedeUserID`）。
2. 写进 `.env`（如 `BILI_COOKIE=...`），请求头带上即可。
3. WBI 密钥用 `/x/web-interface/nav` 接口就能匿名获取（`backend/crawler/wbi.py` 已实现，不需要浏览器）。

### 2.4 优缺点

**优点**
- 无浏览器、无 Playwright、无子进程编排，一轮爬取从"分钟级 × 14 次浏览器启动"降到**秒级**；
- 结构化 JSON 直出，不用再解析 MediaCrawler 的落盘文件；
- 天然适合定时无人值守；
- `backend/` 目录已有 80% 代码（视频/动态解析、去重入库、WBI 签名全都写好了），改造量小。

**缺点 / 风险**
- **Cookie 有效期**：`SESSDATA` 一般可用数月，但过期后需要手动重新复制一次（可做检测：接口返回 `-101` 未登录时在前端提示）；
- **风控面**：纯 HTTP 无浏览器指纹，B站近年对高频纯 API 请求风控趋严（`-352`、`412`）。缓解手段：请求频率放慢（本项目 7 个 UP主、每天 2 轮，量极小，风险不大）、UA 与拿 Cookie 的浏览器保持一致、带全 `buvid3`/`bili_ticket`；
- 用的是**你自己账号的 Cookie**，若真触发风控会影响你的账号（但同样的风险方案B更甚）。

---

## 3. 方案B：CDP 连接你已登录的浏览器

### 3.1 支持情况

MediaCrawler **原生支持**，不用写代码，改 `external/MediaCrawler/config/base_config.py` 两处即可：

```python
ENABLE_CDP_MODE = True        # 当前是 False
CDP_CONNECT_EXISTING = True   # 已经是 True（默认）
```

### 3.2 使用前提（文档：`docs/CDP模式使用指南.md`）

1. Chrome **144+**；
2. 在 `chrome://inspect/#remote-debugging` 勾选 "Allow remote debugging"（端口 9222）；
3. 每次程序连接时，**Chrome 会弹出确认对话框，需要人工点"接受"**（Chrome 136+ 的安全机制，远程调试不再提供免确认的 HTTP 接口，代码里 `_connect_via_cdp` 直连 `ws://localhost:9222/devtools/browser` 后就是在等这个确认）。

### 3.3 为什么不适合本项目 —— 三个硬伤

1. **无人值守不成立**：本项目是 8:00/18:00 定时任务 + 每轮 14 个子进程。每个子进程都是一次新的 CDP 连接，都可能触发浏览器确认弹窗（60 秒超时）；你不在电脑前，整轮全部超时失败。
2. **要求你的浏览器常开**且始终开着调试端口——调试端口本身是个本地安全隐患（任何本机进程都能借它读你浏览器的全部数据）。
3. **主账号风险最大化**：爬取行为直接发生在你日常主力账号、主力浏览器指纹上，一旦风控是对你真实账号处理。

### 3.4 一个代码级的实际风险

读了 `tools/cdp_browser.py` 的实现：连接已有浏览器时，它会复用 `browser.contexts[0]`——也就是**你正在用的那个真实浏览器上下文**（`_create_browser_context()`，L372-375）。而爬虫结束时 `cleanup()` 会调用 `browser_context.close()`（L446-453）。对 `connect_over_cdp` 拿到的真实默认 context 执行 close，**有把你当前所有标签页关掉的风险**。进程清理那步倒是做了保护（`CDP_CONNECT_EXISTING` 时跳过 kill 进程，L491），但 context 关闭没有豁免。要用这个模式得先改这段代码。

### 3.5 适用场景

方案B 的正确用法是**人在电脑前的手动/调试场景**（比如扫码风控绕不过去时手动跑一次），不是定时监控的架构选择。

---

## 4. 实测：`bilibili-api-python` 17.4.2 功能验证（2026-07-28）

测试环境：`tmp_biliapi/`（独立 venv，Python 3.13），测试脚本 `test_api.py` / `test_content.py`。注意：测试期间后台正好有一轮 MediaCrawler 爬取在跑（同 IP 并发），风控结果偏悲观。

### 4.1 安装体验

- PyPI 包仍可正常安装（`pip install bilibili-api-python`，17.4.2，要求 Python ≥3.10）；
- 需额外装一个 HTTP 客户端（`curl_cffi` / `httpx` / `aiohttp` 三选一）；
- 坑：`curl_cffi` 在含中文的路径下证书加载失败（本项目路径含“学习”），需 `select_client("httpx")` 切换。

### 4.2 匿名（无 Cookie）功能测试结果

| 功能 | 结果 | 说明 |
|---|---|---|
| `get_user_info` 用户信息 | ✅ 可用 | 名字/头像/简介齐全 |
| `get_relation_info` 粉丝数 | ✅ 可用 | follower 字段直接可用 |
| `get_videos` 视频列表 | ⚠️ 时好时坏 | 成功时数据完整（bvid/标题/播放/时间戳）；但频繁请求会 412 |
| `get_dynamics_new` 动态（新接口） | ⚠️ 时好时坏 | 成功时返回完整 items；风控时会“软失败”返回空列表（不报错！） |
| `get_dynamics` 动态（旧接口） | ❌ 已死 | B站已下线该接口，永远 404 |
| `get_up_stat` 播放/阅读总量 | ❌ 需登录 | 强制要求 `bili_jct`（必须带 Cookie） |

批量跑 7 个 UP主（间隔 3 秒）：3 个成功、4 个 412；且成功的里面有 2 个动态返回空列表（软风控）。**结论：匿名模式不能满足本项目的监控需求，必须带登录 Cookie。**（与报告第 2 节的判断一致）

风控缓解选项：库自带 `request_settings.set_enable_bili_ticket(True)`、自动 buvid、代理设置；但根本解法还是传入 `Credential(sessdata, bili_jct, buvid3)`。

### 4.3 带 Cookie 测试（待补）

测试时 MediaCrawler 正在跑一轮爬取，登录态 profile 被占用，无法导出 Cookie；且同 IP 并发会污染风控结果。待爬取空闲后可用 `tmp_biliapi/export_cookies.py` 导出 MediaCrawler 已保存的登录态，或直接从自己浏览器 F12 复制 `SESSDATA/bili_jct/buvid3` 写入 `tmp_biliapi/cookies.json` 后重跑 `test_content.py`。

### 4.4 维护状态提醒

- GitHub 仓库 Nemo2011/bilibili-api 已于 2026-07-06 收到 B站律师函并永久关停，17.4.2（2026-06-19）是最后一个版本；
- 接口文档项目 bilibili-API-collect 也已于 2026-01-28 因律师函关停；
- 旧动态接口 404 就是“无人维护后接口腐化”的实例：今后 B站每改一个接口，这个库对应功能就永久坏掉。

---

## 5. 折中方案（顺带说明）

MediaCrawler 还支持 `--lt cookie`（`LOGIN_TYPE="cookie"` + `COOKIES="..."`）：把你浏览器的 Cookie 粘到配置里，跳过扫码。但它**仍然会启动自己的 Chromium**（浏览器还要负责提供 WBI 密钥），只是省掉首次扫码。所以它解决的是"扫码烦"，不解决"开浏览器重"，价值不大。

---

## 6. 最终建议

1. **主力：方案A（纯 API + 浏览器导出 Cookie）**
   - 复用 `backend/crawler/`（wbi.py 原样可用），把 Cookie 从 `.env` 注入 `REQUEST_HEADERS`；
   - 接入现有 `server/` 的数据库和调度（替换 `crawler_runner._run_one` 的子进程调用）；
   - 加上 `-101`（Cookie 过期）与 `-352`（风控）的识别，写入现有的 `last_error` / `login_required` 状态，前端已有展示位。
2. **回退：保留现有 MediaCrawler 链路**——当纯 API 被风控时自动或手动切回（它的浏览器指纹更完整，扫一次码能顶很久）。
3. **不建议：方案B（CDP 连已有浏览器）作为常规链路**——与定时无人值守冲突，且需先修 `cleanup()` 的 context 关闭问题。可以只把它当作偶发风控时的手动救急手段。

### 涉及的关键文件索引

| 文件 | 说明 |
|---|---|
| `server/crawler_runner.py` | 现状：每 UID 起 MediaCrawler 子进程 |
| `external/MediaCrawler/config/base_config.py` | `ENABLE_CDP_MODE` / `HEADLESS` / `LOGIN_TYPE` 等开关 |
| `external/MediaCrawler/tools/cdp_browser.py` | CDP 连接实现（含 3.4 提到的 context.close 风险点） |
| `external/MediaCrawler/media_platform/bilibili/client.py` | 证明浏览器只用于取 Cookie + WBI key，抓取走 httpx |
| `backend/crawler/bilibili.py` + `backend/crawler/wbi.py` | 纯 API 方案的现成代码（缺 Cookie 注入） |
