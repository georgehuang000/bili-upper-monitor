# 对比报告：`bilibili-api-python` 库 vs 自研实现（backend/crawler）

> 调研时间：2026-07-28。对比对象：
> - **库**：`bilibili-api-python` 17.4.2（Nemo2011/bilibili-api，最后一个版本，仓库已关停）
> - **自研**：本项目 [backend/crawler/bilibili.py](backend/crawler/bilibili.py)（约 400 行）+ [backend/crawler/wbi.py](backend/crawler/wbi.py)（约 90 行）
>
> 结论先行：两者是**同一条技术路线**（不开浏览器、httpx 直接调 B站 Web API、WBI 签名），核心接口甚至完全相同。差距只在一个地方——**反风控的工程细节**。这恰好解释了为什么你的自研版当年容易被 -352/412 拦，而库相对稳一些。

---

## 0. 一句话总结

| | 自研 `backend/crawler` | 库 `bilibili-api-python` |
|---|---|---|
| 技术路线 | httpx 直调 API + WBI 签名 | **完全一样** |
| 视频接口 | `/x/space/wbi/arc/search` | **完全一样** |
| 动态接口 | `/x/polymer/web-dynamic/v1/feed/space` | **完全一样**（`get_dynamics_new`） |
| WBI 签名 | ✅ 自己实现（正确） | ✅ 实现（多了 `web_location` 参数） |
| **buvid3/buvid4 自动获取** | ❌ 没有 | ✅ 有（`get_buvid` + SPI 激活） |
| **bili_ticket 签名** | ❌ 没有 | ✅ 有（HMAC-SHA256） |
| **-403 自动重算 WBI 重试** | ❌ 没有 | ✅ 有（默认 3 次） |
| **登录 Cookie 自动刷新** | ❌ 没有 | ✅ 有（`check_refresh`） |
| **dm_img 反爬参数** | ❌ 没有 | ✅ 有 |
| 依赖 & 维护 | 只依赖 httpx，自己可控 | 已停止维护，接口腐化无人修 |

**你的实现不是"不能用"，而是"缺了几个反风控字段"**。补上这几个字段，自研版就能达到和库同等的稳定度，且没有第三方停更风险。

---

## 1. 相同的部分（技术路线一致）

两者都走：**httpx 直接请求 B站 Web API**，浏览器完全不参与数据抓取。用到的接口都一样：

| 用途 | 接口 | 自研位置 | 库位置 |
|---|---|---|---|
| 视频列表 | `/x/space/wbi/arc/search` | `fetch_videos()` | `User.get_videos()` |
| 动态列表 | `/x/polymer/web-dynamic/v1/feed/space` | `fetch_dynamics()` | `User.get_dynamics_new()` |
| 用户信息 | `/x/space/wbi/acc/info` | `fetch_user_info()` | `User.get_user_info()` |
| 粉丝数 | `/x/relation/stat` | `fetch_user_fans()` | `User.get_relation_info()` |
| WBI 密钥源 | `/x/web-interface/nav` | `wbi.update_keys()` | 内部 `_get_mixin_key()` |

连 WBI 签名算法都一样：都是"混淆表重排 img_key+sub_key → 取前 32 位 → 参数排序拼接 → MD5"。你的 [wbi.py](backend/crawler/wbi.py) 的 `MIXIN_KEY_ENC_TAB` 和 `enc_wbi()` 是**正确**的。

---

## 2. 关键区别：库多做的反风控工程（这是稳定性差距的根源）

我读了库的 `utils/network.py`，它在每个请求前多注入了几层你没有的东西：

### 2.1 buvid3 / buvid4 自动获取与激活（你没有）

- 库：请求前若无 buvid，会调 SPI 接口 `_get_spi_buvid()` 拿到 `b_3`/`b_4`，再调 `_active_buvid()` **主动激活**，然后带进 Cookie（`network.py` L2033-2051）。
- 你：`REQUEST_HEADERS` 里**完全没有 buvid3**。而 B站动态接口 `feed/space` 匿名 + 无 buvid3 直接 `-352`。**这就是你自研版当年拉动态失败的直接原因之一。**

### 2.2 bili_ticket 签名（你没有）

- 库：用 `HMAC-SHA256("XgwSnGZ1p", "ts{时间戳}")` 生成签名，换取 `bili_ticket`（3 天有效，`network.py` L1967-1991、L2054-2070），可显著降低 412。开关 `request_settings.set_enable_bili_ticket(True)`。
- 你：没有。

### 2.3 -403 自动重算 WBI 并重试（你没有）

- 库：请求返回 `-403` 时，自动 `recalculate_wbi()` 重新拉密钥并重试，默认 3 次（`network.py` L2365-2382）。
- 你：`wbi.py` 里密钥缓存 1 小时，密钥过期/失效时**不会重试**，直接失败。

### 2.4 登录 Cookie 自动刷新（你没有）

- 库：`Credential.check_refresh()` / `_refresh_cookies()` 能检测 Cookie 将过期并自动续（`network.py` L1348、L1459）。
- 你：无 Cookie 概念（连登录态都没接入）。

### 2.5 dm_img 行为参数（你没有）

- 库：部分接口注入 `dm_img_list`/`dm_img_str`/`dm_cover_img_str` 模拟鼠标键盘轨迹（`network.py` L1940-1950）。
- 你：没有。

---

## 3. 自研实现自身的问题（与库无关，但值得记录）

1. **根本没带登录态**：`REQUEST_HEADERS` 只有 UA/Referer，无 Cookie。动态接口必登录，所以自研版对动态基本抓不到。
2. **UID 有 3 个没解析**（config.py L27-29 的 uid=0），依赖 `search_user_by_name` 运行时搜索，而搜索接口本身也要 Cookie，容易连环失败。
3. **静默降级**：`wbi.update_keys()` 和各 `fetch_*` 里大量 `except: pass`，出错时返回空而不报错——和库的动态"软失败返回空列表"是同类隐患，容易让你误以为"这个 UP 没更新"。
4. **数据库不一致**：自研用 `monitor.db` + `UPPERS`，而线上跑的 `server/` 用的是 `data.db` + `UP_NAMES`。自研这套实际上已经被 `server/` + MediaCrawler 取代了。

---

## 4. 三种方案横向定位

| | 自研 backend（现状） | 库 bilibili-api-python | server/ + MediaCrawler（线上在跑） |
|---|---|---|---|
| 开浏览器 | ❌ 不开 | ❌ 不开 | ✅ 每 UID 开无头浏览器 |
| 反风控完整度 | 低（缺 buvid/ticket/重试） | 中高（工程齐全） | 高（真浏览器指纹 + Cookie） |
| 登录态 | 未接入 | 传 Credential | 扫码一次自动复用 |
| 第三方存亡风险 | 无（自己代码） | 高（已停更） | 中（MediaCrawler 仍维护） |
| 速度 | 最快 | 最快 | 最慢 |

---

## 5. 建议

**不建议直接引入这个库**（已停更，接口腐化只会越来越多），但**强烈建议把库的反风控细节移植进自研版**，性价比最高：

1. 给 [backend/config.py](backend/config.py) 的 `REQUEST_HEADERS` 补 **Cookie**（`SESSDATA/bili_jct/buvid3`，从浏览器导出）；
2. 移植 **buvid3/buvid4 自动获取 + bili_ticket 签名**（参考 `network.py` 的 `get_buvid`/`_get_bili_ticket`，各约 20 行）；
3. 给 [wbi.py](backend/crawler/wbi.py) 加 **-403 重算重试**；
4. 把 `except: pass` 改成**区分 -352/-403/-101 错误码**并写入 `server/` 已有的 `last_error`/`login_required` 状态；
5. 统一到 `server/` 的 `data.db`，废弃 `backend/monitor.db` 那套。

这样得到的是：**不开浏览器 + 秒级 + 反风控齐全 + 无第三方依赖风险**的自研版，正好是报告《爬取方案调研报告.md》里推荐的"方案A"的落地形态。
